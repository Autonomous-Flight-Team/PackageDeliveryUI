# Running Source
If you want to run the project from source, first install and open the `Package Delivery UI` folder in VSCode.

Next, install maven.

Finally, to run the project, run `mvn javafx:run` from the `Package Delivery UI` folder.